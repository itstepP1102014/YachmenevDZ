#include <stdio.h>
#include <stdlib.h>
#incclude <time.h>
int main()
{
    srand(time(NULL));
    char answer;
    comp=rand()%99+1
   do
   {
       printf("%d\n", &comp);
       printf("This number is > then you?  Y\N"\n, answer);
       scanf(" %c", &answer)

   }
   if (answer=='Y')
    {
        comp
    }
    if (i==human)
    {
      printf("You are won!")
    }
    return 0;
}
